version: 2.1

orbs:
  docker: circleci/docker@1.2.1

workflows:
  build-and-publish-docker-image:
    jobs:
      - docker/publish:
          image: shiny-wordcloudgenerator/web
          registry: registry.heroku.com
